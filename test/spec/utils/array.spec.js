let expect = require('chai').expect;
let array = require('../../src/utils/array');

describe('utils/array', function () {

    it('indexOf(arr, item)', function () {
        expect(1).to.be.a('number');
    });
});
